# 🤖 AI-Фриланс Ассистент - Telegram Bot

**Версия:** Prototype 0.1  
**Описание:** Бот симулирует работу AI-ассистента для автоматизации откликов на фриланс-биржах

---

## 📦 Установка

### 1. Установи зависимости

```bash
pip install -r requirements.txt
```

Или установи напрямую:
```bash
pip install aiogram==3.13.1
```

### 2. Получи токен бота

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Отправь команду `/newbot`
3. Следуй инструкциям (придумай имя и username для бота)
4. Скопируй полученный токен

### 3. Настрой бота

Открой файл `bot.py` и замени строку:
```python
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
```

На свой токен:
```python
BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
```

---

## 🚀 Запуск

Запусти бота командой:

```bash
python bot.py
```

Если все настроено правильно, увидишь:
```
🤖 Бот запущен!
Нажми Ctrl+C для остановки
```

---

## 🧪 Тестирование

### Команды для проверки:

1. **Запуск бота**
   ```
   /start
   ```
   Должно появиться приветствие и список команд

2. **Просмотр заданий**
   ```
   /tasks
   ```
   Покажет 5 тестовых заданий

3. **Отправка отклика**
   ```
   /respond 1
   ```
   Отправит отклик на задание с ID=1, начислит +50₽

4. **Проверка баланса**
   ```
   /balance
   ```
   Покажет текущий баланс и количество откликов

5. **История откликов**
   ```
   /my_responses
   ```
   Покажет все отправленные отклики

6. **Справка**
   ```
   /help
   ```
   Покажет список команд

### Сценарий полного теста:

```
/start          → Приветствие
/tasks          → Список из 5 заданий
/respond 1      → Отклик на задание 1 (+50₽)
/respond 2      → Отклик на задание 2 (+50₽)
/balance        → Баланс: 100₽, откликов: 2
/my_responses   → История 2 откликов
/respond 1      → Ошибка: уже откликался
```

---

## 📁 Структура проекта

```
.
├── bot.py              # Основной код бота
├── requirements.txt    # Зависимости Python
├── user_data.json      # База данных пользователей (создается автоматически)
└── README.md          # Эта инструкция
```

---

## 🔧 Как это работает

### Архитектура:

1. **Данные пользователей** хранятся в `user_data.json`:
   - Баланс
   - История откликов
   - Дата регистрации

2. **Задания** хранятся в коде как список словарей (в будущем — API бирж)

3. **AI-генерация** пока работает через шаблоны (в версии 0.2 будет реальная AI)

### Основные функции:

- `load_user_data()` — загрузка данных из JSON
- `save_user_data()` — сохранение данных в JSON
- `get_user()` — получение/создание пользователя
- `generate_response()` — генерация отклика (заглушка)

---

## 🛠️ Возможные проблемы

### Ошибка: "Invalid token"
- Проверь, что токен скопирован правильно
- Убедись, что нет лишних пробелов

### Ошибка: "Module not found"
- Установи зависимости: `pip install -r requirements.txt`

### Бот не отвечает
- Проверь, что бот запущен (`python bot.py`)
- Проверь интернет-соединение

---

## 📋 TODO: Версия 0.2

- ✅ Базовый функционал (v0.1)
- ⏳ Интеграция с API фриланс-бирж
- ⏳ Реальная AI-генерация (GPT/YandexGPT)
- ⏳ База данных PostgreSQL
- ⏳ Система авторизации
- ⏳ Монетизация и вывод средств
- ⏳ Веб-интерфейс
- ⏳ Аналитика и статистика

---

## 📞 Поддержка

Если возникли вопросы или нашел баг — создай issue или напиши разработчику.

**Удачи в тестировании! 🚀**
