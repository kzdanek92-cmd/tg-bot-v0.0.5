# Version 0.0.5 - Full Payments & Integration Update

## 🚀 Обновление до версии 0.0.5

Эта версия добавляет **полную поддержку платежных систем**: CryptoBot (TON, USDT, BTC) и FreeKassa.

---

## 📋 Требования перед установкой

### 1. Переменные окружения (.env)

Обновите ваш `.env` файл следующими переменными:

```dotenv
# Telegram
BOT_TOKEN=<ваш_токен_бота>

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=<ваш_anon_ключ>

# CryptoBot (для TON, USDT, BTC)
CRYPTOBOT_TOKEN=<ваш_cryptobot_токен>

# FreeKassa (опционально)
FREEKASSA_MERCHANT_ID=<ваш_merchant_id>
FREEKASSA_SECRET1=<ваш_secret1>
FREEKASSA_SECRET2=<ваш_secret2>

# Логирование
LOG_LEVEL=INFO
```

### 2. Создание таблицы payments в Supabase

Откройте Supabase SQL Editor и выполните SQL из файла:
```
database/migrations/001_create_payments_table.sql
```

Или выполните руками в SQL редакторе Supabase Console:
- Перейдите в `SQL Editor`
- Выполните содержимое файла `001_create_payments_table.sql`
- Таблица `payments` будет создана со всеми необходимыми индексами и триггерами

---

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

Если `aiohttp` не установлен:
```bash
pip install aiohttp==3.9.1
```

---

## 🏃 Запуск бота

```bash
python bot.py
```

Бот автоматически запустит:
- Telegram bot polling
- HTTP callback сервер (порт 8080) для приёма webhook'ов от:
  - CryptoBot (`/webhook/cryptobot`)
  - FreeKassa (`/webhook/freekassa`)

---

## 🔑 Как получить токены

### CryptoBot API

1. Откройте [@CryptoBot](https://t.me/CryptoBot) в Telegram
2. Нажмите на 3 точки (меню) → "Crypto Pay"
3. Следуйте инструкциям
4. Скопируйте API Token в `.env` как `CRYPTOBOT_TOKEN`

### FreeKassa

1. Зарегистрируйтесь на https://www.free-kassa.ru/
2. Создайте проект/магазин
3. В настройках найдите:
   - `MERCHANT_ID`
   - `SECRET_WORD_1` (SECRET1)
   - `SECRET_WORD_2` (SECRET2)
4. Скопируйте в `.env` файл

---

## 💳 Как использовать платежи

### Для пользователя

1. Нажать кнопку **"💳 Пополнить баланс"**
2. Выбрать способ оплаты:
   - **🪙 Оплатить TON**
   - **💵 Оплатить USDT**
   - **₿ Оплатить Bitcoin**
   - **💳 Оплатить через FreeKassa**
3. Выбрать сумму
4. Получить ссылку и перейти к оплате
5. После оплаты нажать **"🔍 Проверить оплату"**
6. Баланс автоматически пополнится

### Для разработчика

Структура сохранения платежа в Supabase:

```python
{
    "user_id": 12345,                    # Telegram User ID
    "currency": "TON",                   # Валюта: TON, USDT, BTC, RUB
    "amount": 10.5,                      # Сумма в валюте
    "tx_id": "invoice_12345_1234567890", # Уникальный ID или invoice ID
    "status": "paid",                    # pending, paid, failed, expired
    "meta": {...},                       # Доп данные (invoice JSON или др)
    "created_at": "2025-11-15T10:30:00"
}
```

---

## 🔗 Настройка webhook'ов

### CryptoBot Webhook

1. В личном кабинете CryptoBot настройте:
   - **URL:** `https://your-domain.com/webhook/cryptobot`
   - **Метод:** POST
   - **Content-Type:** application/json

2. Бот будет автоматически обновлять статус платежа при получении уведомления

### FreeKassa Webhook

1. В личном кабинете FreeKassa:
   - **Адрес обработчика:** `https://your-domain.com/webhook/freekassa`
   - **Метод:** POST

2. При оплате FreeKassa отправит callback с подписью, которая будет проверена

---

## 🧪 Локальное тестирование (без ngrok)

Для локального тестирования без публичного IP можно использовать **ngrok**:

```bash
# Установка ngrok
choco install ngrok  # Windows с Chocolatey

# или скачайте с https://ngrok.com

# Запуск туннеля
ngrok http 8080

# Вы получите URL вроде: https://xxxx-xx-xxx-xxx.ngrok.io
# Используйте его как https://xxxx-xx-xxx-xxx.ngrok.io/webhook/cryptobot
```

---

## 📊 Структура файлов

```
bot_v0.0.2/
├── bot.py                      # Главный файл бота (с HTTP сервером)
├── config.py                   # Конфигурация + переменные окружения
├── requirements.txt            # Зависимости
├── database/
│   ├── models.py              # Модели данных (User, Payment, etc)
│   ├── supabase_client.py     # Клиент Supabase + методы платежей
│   ├── migrations/
│   │   └── 001_create_payments_table.sql  # Миграция для payments
│   └── schema.sql
├── payments/
│   ├── crypto.py              # CryptoBot API (TON, USDT, BTC)
│   ├── freekassa.py           # FreeKassa API
│   └── __init__.py
├── handlers/
│   ├── payments_handler.py    # Все обработчики платежей
│   ├── start_handler.py       # Команда /start
│   ├── profile_handler.py     # Профиль пользователя
│   ├── balance_handler.py     # Проверка баланса
│   ├── tasks_handler.py       # Задания
│   ├── callback_handler.py    # Обработка callback'ов
│   └── info_handler.py        # Информационные разделы
├── services/
│   ├── user_service.py        # Сервис пользователей
│   ├── ai_service.py          # AI сервис
│   └── task_service.py        # Сервис заданий
├── ui/
│   └── menus.py               # Все меню и клавиатуры
├── utils/
│   └── error_handler.py       # Обработка ошибок
├── docs/
│   ├── CHANGELOG_v0.0.0.4.md
│   ├── CHANGELOG_v0.0.0.5.md  # Новое!
│   └── ...
└── .env                        # Переменные окружения (не в git!)
```

---

## 🐛 Отладка

### Включение debug логирования

```python
# В bot.py или config.py
LOG_LEVEL=DEBUG
```

### Проверка логов

Логи сохраняются в файл `bot.log`:
```bash
tail -f bot.log  # Unix/Mac
Get-Content bot.log -Tail 20 -Wait  # PowerShell
```

### Проверка webhook'ов

Тестовый запрос к webhook:

```bash
# CryptoBot
curl -X POST http://localhost:8080/webhook/cryptobot \
  -H "Content-Type: application/json" \
  -d '{"update_type":"invoice_paid", "payload":{"invoice_id": 12345}}'

# FreeKassa
curl -X POST http://localhost:8080/webhook/freekassa \
  -d "m=123&oa=100&o=order_123&s=hash_here"
```

---

## ✅ Проверка работы

### Тест 1: Регистрация пользователя
```
1. Написать /start боту
2. Должны получить главное меню
3. В БД должен создаться record в таблице users
```

### Тест 2: Оплата TON (CryptoBot)
```
1. Нажать "💳 Пополнить баланс"
2. Выбрать "🪙 Оплатить TON"
3. Выбрать сумму (например, 1 TON)
4. Получить invoice и ссылку
5. В таблице payments должна появиться запись со статусом 'pending'
6. После оплаты (реальной или имитации) статус должен измениться на 'paid'
```

### Тест 3: Проверка оплаты
```
1. Нажать "🔍 Проверить оплату"
2. Бот должен найти последний pending платёж
3. Если статус paid — сообщение: "✅ Оплата подтверждена"
4. Если статус pending — сообщение: "⏳ Платеж в обработке"
```

---

## 📝 Примечания

- **Безопасность:** Все ключи должны храниться в `.env` (не коммитить в git!)
- **Production:** Для production используйте webhook'и (не polling) и HTTPS
- **Курсы валют:** Текущая версия использует примерные курсы:
  - 1 TON ≈ 50 RUB (можно обновить в config)
- **Retry логика:** Для production рекомендуется добавить retry при ошибках Supabase
- **Рейт-лимиты:** CryptoBot имеет ограничения на количество запросов

---

## ✅ Выполненные версии

### v0.0.5 — Full Payments & Integration Update ✅ ЗАВЕРШЕНА
**Дата:** 15.11.2025  
**Статус:** 🚀 Production Ready

Реализованные функции:
- [x] CryptoBot для TON, USDT, BTC
- [x] FreeKassa интеграция (работает)
- [x] Система управления платежами в БД
- [x] Конвертация валют (CoinGecko)
- [x] HTTP webhook сервер (aiohttp)
- [x] История платежей в Supabase

---

## 🔜 Планы на будущее (v0.0.6)

- [ ] Redis кеширование для pending_invoices
- [ ] Улучшенная обработка ошибок
- [ ] Webhook signature verification (HMAC)
- [ ] Автоматическая конвертация курса из API
- [ ] Система рефералов
- [ ] Экспорт истории платежей
- [ ] Admin панель для управления платежами

---

## 📞 Контакты и поддержка

Если возникли вопросы:
- Telegram: @Danyadlyalubvi2
- GitHub Issues: (если есть)
- Email: (если указан)

---

**Версия:** 0.0.5  
**Дата:** 15.11.2025  
**Статус:** 🚀 Ready to Deploy
